#include "monty.h"
/**
  *nop- nothing
  *@head: head f stack
  *@line_number: line_number
  *Return: no return
 */
void nop(stack_t **head, unsigned int line_number)
{
	UNUSED(line_number);
	UNUSED(head);
}
